name: bank_data_repository
description: Dart package which manages the bank data domain.
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  equatable: ^2.0.3

  # Network dependencies
  http: ^0.13.0
  stream_transform: ^2.0.0